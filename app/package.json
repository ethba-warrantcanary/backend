{
  "name": "warrantcanary-dashboard",
  "version": "1.0.0",
  "description": "A frontend to an Ethereum based warrant canary",
  "main": "canary.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "web3": "^0.17.0-alpha"
  }
}